# Deny all incoming traffic to mock-attacker from the bank's namespaces.
# This is what's APPLIED by ./scripts/policies-on.sh and REMOVED by
# ./scripts/policies-off.sh. Without it, currency-converter' lateral exfil to
# mock-attacker succeeds (Act 1). With it, ztunnel resets the
# connection at L4 and customer data never leaves the cluster (Act 2).

apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: deny-bank-to-attacker
  namespace: external-attacker
spec:
  action: DENY
  rules:
    - from:
        - source:
            # Wildcard match across every bank-side namespace + the platform.
            # Stronger than an explicit list because new namespaces added
            # later automatically inherit the protection (no policy churn
            # required when an extra MCP namespace shows up).
            namespaces:
              - "trustusbank-bank-*"
              - "trustusbank-platform"
