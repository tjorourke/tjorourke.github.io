<?xml version="1.0" encoding="UTF-8"?>
<!-- Sitemap index. The site itself is published from the tjorourke/solo repo,
     which GitHub Pages serves under /solo/, so its sitemap cannot sit at the
     host root. Crawlers only fetch robots.txt from the root, so this index is
     what makes the real sitemap reachable by convention. -->
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <sitemap>
    <loc>https://www.masterthemesh.com/solo/sitemap.xml</loc>
  </sitemap>
</sitemapindex>
